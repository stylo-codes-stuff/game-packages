class Brownian_noisemap{
    constructor(width,height,seed,amplitude,frequency,amplitude_multiplier,frequency_multiplier){
        const gen = lcg(seed);
        this.width = width;
        this.height =height;

    }
}
class noisemap{
   constructor(width,height,seed){} 
}